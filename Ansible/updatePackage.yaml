---

- name: upgrade all packages
  yum:
    name: '*'
    state: latest
- name: Install following packages
  yum:
    name: "{{ packages }}"
  vars:
    packages:
    - tomcat
    - python3
    - python3-pip
    - docker-engine
    - git
